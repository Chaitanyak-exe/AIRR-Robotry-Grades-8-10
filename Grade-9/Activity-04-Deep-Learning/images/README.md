# Images

Add real photos or screenshots after performing the activity. Suggested files:

- `setup.jpg` — laptop and editor showing `code/main.py` open
- `install.jpg` — terminal showing the library install commands and success messages
- `training.jpg` — terminal showing the program output including "Neural network trained successfully!" and the prediction table
- `experiment.jpg` — screenshot of changed parameters (for example hidden_layer_sizes) and results

Do not add fake images. Use clear, well-lit photos or readable terminal screenshots.
